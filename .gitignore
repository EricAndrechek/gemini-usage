# Binaries (only in repo root, not cmd/ subdirectories)
/gemini-usage
/gemini-cookies

# Python
_python/.venv/
.venv/
__pycache__/
*.egg-info/

# Recon output (large JSON dumps from API exploration)
_docs/recon/*.json

# Claude Code
.claude/
